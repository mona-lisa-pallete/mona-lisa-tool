import React from "react";
import { Button, View, Image } from "@tarojs/components";

import "./index.less";

/**
 * props 由自定义的 form 表单传入
 */
interface DDDButton111Props {}

const DDDButton111: React.FC<DDDButton111Props> = (props) => {
  return <View className=""></View>;
};

export default DDDButton111;
